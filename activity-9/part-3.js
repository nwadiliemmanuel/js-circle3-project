//Debugging Chhallange

//1
const greetUser = (name) => {
    console.log("Welcome," + name );
    
}
greetuser("Emeka")



//First code snippet is correct logically, but the function when it is called should make the code run is not properly typed, hence the function will be flagged as undefined. In otherwords, instead of having greetuser(“Emeka”); you should have greetUser(“Emeka”);





//2
const subtract = (a, b) => a - b
console.log(subtract(10, 4));



//Second code snippet is correct; it is simply an arrow function. The only issue that may be spotted is the lack of proper usage of punctuation marks
