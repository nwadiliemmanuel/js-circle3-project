1.
//A program might need `Math.random()` when it needs to make a random or unpredictable choice. Randomness can make programs more interesting and less predictable. Games, dice and card games and random selections are  examples of programs that can use randomness.

//console.log(Math.random());


2.
//The `includes()` string method can help check whether a letter or a text are are present in a specific word or character. For example, `email.includes("@")` checks whether the email contains the `@` symbol. It returns `true` if it is found and `false` if it is not.

//let email = "emmanuel@gmail.com";
//console.log(email.includes("@")); output: true



3.
//`.slice()` takes a portion of a string and produces another string. For example, 
// 
//let word ="I love you";
//console.log(word.slice(0, 6)); Result: I love you


//`.split()` breaks a string into separate pieces based on a separator and produces an array. 
 //let sentence = "I love you"
//console.log(sentence.split(" "));// Result: [ 'I', 'love', 'you' ]


//The main difference is that `.slice()` produces a string, while `.split()` produces an array.



