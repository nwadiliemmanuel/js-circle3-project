// Snippet A

let score = 72;
let result = score >= 50 ? "Pass" : "Fail";
console.log(result); // Output: Pass

// Snippet B 

let cartTotal = 0;
let message = cartTotal > 0 ? "Proceed to checkout" : "Your cart is empty";
console.log(message); // Output: Your cart is empty 

// Snippet C

let stock = 5;
console.log(`Stock status: ${stock > 0 ? "In stock" : "Out of stock"}`); // Output: Stock status: In stock      